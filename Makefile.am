## Process this file with automake to produce Makefile.in.
## MX-TCE: Makefile.am

SUBDIRS = src . doc

clean:
	-rm -rf *.log

distclean: 
	-rm -rf configure Makefile Makefile.in stamp-h* config.h*
	-rm -rf aclocal.m4 autom4te.cache config.log config.status 

