-- runyaml.pl --
A perl script to parse a yaml file that is of OSCARSv6 createResv format. 
It launch the mxtce_test API client with the parameters loaded from yaml.
To run:
	perl runyaml.pl example1.yaml 
